import { defineComponent } from 'vue'

const select = defineComponent({
    name: 'select',
    props: {

    },
    setup() {
        return () => (
            <div>select</div>
        )
    }
})

export default select