import { defineComponent } from 'vue'

const pagination = defineComponent({
    name:'pagination',
    props:{

    },
    setup() {
        return () => (
            <div>pagination</div>
        )
    }
})

export default pagination